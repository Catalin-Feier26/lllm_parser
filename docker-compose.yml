services:
  perl-parser:
    build: .
    container_name: thesis-parser
    volumes:
      # Mount entire project directory for live code editing
      - .:/app
    stdin_open: true
    tty: true
    working_dir: /app
